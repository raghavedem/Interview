name: Hello World

on: 
  push:
   branches: [main]

jobs:
  say-hello: 
    runs-on: ubuntu-latest
    steps: 
      name: Checkout the Code
      run:  actions/checkout@v3
      name: Print Hello
      run:  echo "Hello Github Actions"
